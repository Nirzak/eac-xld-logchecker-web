services:
  logchecker:
    image: ghcr.io/nirzak/audio-ripping-logchecker:latest
    container_name: logchecker
    environment:
      - PUID=1002
      - PGID=1002
      - SUBPATH=/logchecker
      - LOG_LEVEL=info
    ports:
      - "5050:5050"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
