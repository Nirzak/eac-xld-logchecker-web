services:
  logchecker-web:
    build: .
    image: ghcr.io/${GITHUB_REPOSITORY:-nirzak/eac-xld-logchecker-web}:latest
    container_name: logchecker-web
    ports:
      - "5050:5050"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
