services:
  logchecker-web:
    build: .
    image: ghcr.io/nirzak/eac-xld-logchecker-web:latest
    container_name: logchecker-web
    environment:
      - PUID=1002
      - PGID=1002
    ports:
      - "5050:5050"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
